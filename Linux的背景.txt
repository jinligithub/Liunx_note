1.操作系统
2.网络
3.组成原理

1.Linux的发展史，
1991年一个大学生（林拉斯，托瓦斯）写了一个小的操作系统。放到网上，有人免费的指出错误，修改错误的意见。（因为很多人喜欢这个）所以操作系统越来越完善，越来越多的人使用它。
centos 6.5.32版本

Linux由C语言写的。
开源：就是把自己的代码放到网上。
开源的好处：a.不同的人参与进来，会发现很多问题并及时的修改问题。b.因为开源所以越来越多的人参与i进来。

2.Linux的特点
开源：免费的，
稳定性好，所有的问题可以被及时发现，即使被修正
数据安全：网络的使用痕迹

3.操作系统的定位：它是一款搞管理的软件。
管理：
1）用户层面上：管理软硬件资源
2）底层：内存管理，文件管理，进程管理

4.操作系统为什要管理好这些？
为了给用户提供一个良好的运行环境（良好：稳定，可靠，便于用户操作）

5.操作系统如何管理硬件资源？
通过设备驱动来管理硬件资源（电脑插入鼠标的过程）


一.指令

1.ls指令
语法：ls  [选项] [目录或文件]

功能：
1）对于文件，列出文件名以及其他信息。
2）对于目录，列出该文件下的所有子目录与文件。

常用选项：

-a ：列出文件下的所有文件，包括隐藏文件。
-d：(ls -d：列出指定文件)
-i：输出文件的i节点（inode:文件的inode编号，一个文件对应的有一个inode）信息。 ls -ai :指定文件
-k：以K字节的形式表示文件的大小，ls  -alk 指定文件
-l：列出文件的详细信息。显示文件的各种属性，权限，大小，时间等
-n：用数字的UID，GID代替名称。
-F：在每个文件名后附上一个字符以说明该文件的类型（“*”表示普通文件；“/”表示是目录；“@”表示是连接；“I”表示FIFOs；“=”表示套接字（sockets））

注意事项
1.".文件名"表示隐藏文件
2.“.”表示当前目录
3.“..”表示上一级目录
4.“/”表示根目录（路径中间的/表示分隔符）（“/”表示根目录因为文件都是从根目录开始的）
5.相对路径：它不是从根目录开始的，所以他不是唯一的
6.绝对路径：它是从根目录开始向后访问目录的路径，它是唯一的


2.pwd指令
显示用户当前所在的目录

3.cd指令（change  dir）
在Linux下，磁盘中的文件和目录组成了一颗目录树，每个节点都是目录或者文件。
语法：cd  目录名
功能：改变工作目录
常用选项

cd  .. ：返回上一级目录
cd /home/......：绝对路径（唯一的，从根目录开始）
cd .../file/：相对路径（不唯一）
cd  - ：返回最近访问的目录
cd  ~：进入用户的家目录（当前用户的主工作目录）

主工作目录：Windows就是我的文档；Linux下就是刚打开Linux后直接pwd,显示出来的就是主工作目录

4.touch 指令
语法：touch  [选项]  文件名
功能：touch 命令参数可以更改文件或目录的日期时间，包括存取时间，或者新建一个不存在的文件。
常用选项


5.mkdir
6cp

7.mv



绝对路径

相对路径


