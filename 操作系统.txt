2.操作系统
操作系统的概念：任何一个计算机系统都包含一个基本的程序集合。
操作系统包含：
	* 内核：进程管理，驱动管理，文件管理，内存管理
	* 其他程序：函数库，shell程序

1.操作系统的目的？
	* 与硬件交互，管理所有的软硬件资源
	* 为用户提供一个良好的执行环境

操作系统的定位：操作系统是一款”搞管理“的软件

2.操作系统管理的方法：
	* 先描述管理对象：可以用结构体描述对象的所有信息
	* 在组织被管理的对象：组织就是用链表或其他高效的结构体，在结构体里添加指针，让他们之间相互联系起来

3.操作系统的搞管理的目的：
	* 提高了计算机的效率。

4.系统调用和库函数概念是什么？
	* 系统调用：从开发的角度，操作系统会对外表现为一个整体，但是会暴露自己的部分接口，供上层开发使用，这部分由操作系统提供的接口称为系统调用。
	* 库函数：在系统调用的使用上，功能较基础，对用户的要求比较高，开发者会把部分的系统调用进行适度封装，从而形成了库。它的存在有利于上层用户或者开发者进行二次开发。


5.操作系统的结构是什么？

6.如何调用操作系统的接口？
	* 操作系统是一个完整的系统，它要使用操作系统的功能只能以接口的形式调用（为了保证系统的完整性）

7.库函数和系统调用接口的区别?
	* 有上下级的区别。库函数可以调用系统调用接口。

